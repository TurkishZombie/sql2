SELECT  username, emailaddress
FROM i_users
WHERE userId=(SELECT max(Counter)
              FROM ((SELECT userId, COUNT(*) AS Counter
                     FROM i_user_login_logs
                     GROUP BY userId)))
